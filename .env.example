# App
APP_ENV=local
APP_HOST=0.0.0.0
APP_PORT=8000

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/pm_os_mvp

# Optional for later LLM features
OPENAI_API_KEY=

# Optional AWS (prefer IAM role on EC2 instead of keys)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

